package com.demodev.fabricsk.packagefile;

public enum PackageType {
   DIRECTORY,
   ZIP,
   HEAVYSK;

   // $FF: synthetic method
   private static PackageType[] $values() {
      return new PackageType[]{DIRECTORY, ZIP, HEAVYSK};
   }
}
